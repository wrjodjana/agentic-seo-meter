{
  "description": "Logs documentation fetched by the agent for aeo-meter",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^WebFetch$|^mcp__.*(fetch|scrape|crawl)",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/python.sh\" \"${CLAUDE_PLUGIN_ROOT}/scripts/log_fetch.py\"",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "^WebFetch$|^mcp__.*(fetch|scrape|crawl)",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/python.sh\" \"${CLAUDE_PLUGIN_ROOT}/scripts/log_fetch.py\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
